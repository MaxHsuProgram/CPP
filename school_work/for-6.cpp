#include<cmath>
#include<iostream>
using namespace std;
int main()
{
    int W,H,i,k;
    cin >> W >> H;
    for(i = 1;i <= H;i++)
    {
        for(k = 1;k <= W;k++)
        {
            cout << "#";
        }
    }
}